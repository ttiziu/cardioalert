# Versiones probadas localmente con Python 3.12 (ver .python-version).
fastapi==0.141.1
uvicorn[standard]==0.53.0
numpy==2.5.3
# El paquete de Linux funciona en CPU; los drivers CUDA solo se instalan con [and-cuda].
tensorflow==2.20.0

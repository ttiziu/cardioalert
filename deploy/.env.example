# Copiar a deploy/.env (Git lo ignora). docker compose lo lee automáticamente.
# Clave que la app debe enviar a la API de IA (misma que ML_API_KEY en la app):
ML_API_KEY=cambia-esta-clave
# Puertos del servidor (opcionales)
BACKEND_PORT=3000
ML_API_PORT=8000
